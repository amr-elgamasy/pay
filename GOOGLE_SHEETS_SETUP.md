# دليل ربط الموقع بـ Google Sheets

## الخطوات بالتفصيل:

### 1️⃣ إنشاء Google Sheet جديد

1. افتح [Google Sheets](https://sheets.google.com)
2. اضغط على **+ Blank** لإنشاء ملف جديد
3. سمّي الملف: **فلوسنا - قاعدة البيانات**

---

### 2️⃣ إنشاء الأوراق (Sheets) المطلوبة

قم بإنشاء 7 أوراق داخل الملف، وسمّيهم بالضبط كالتالي:

- `الإيداعات_المعلقة`
- `الإيداعات_المقبولة`
- `الإيداعات_المرفوضة`
- `المصروفات`
- `السحوبات_المعلقة`
- `السحوبات_المقبولة`
- `السحوبات_المرفوضة`

**ملاحظة:** الأسماء يجب أن تكون بالضبط كما هي مكتوبة أعلاه (مع الشَّرطة السفلية _)

---

### 3️⃣ إضافة العناوين للأعمدة

#### ورقة `الإيداعات_المعلقة`:
```
التاريخ | الاسم | الهاتف | المبلغ | الحالة
```

#### ورقة `الإيداعات_المقبولة`:
```
التاريخ | الاسم | الهاتف | المبلغ | تاريخ الموافقة
```

#### ورقة `الإيداعات_المرفوضة`:
```
التاريخ | الاسم | الهاتف | المبلغ | تاريخ الرفض
```

#### ورقة `المصروفات`:
```
التاريخ | الوصف | الفئة | المبلغ
```

#### ورقة `السحوبات_المعلقة`:
```
التاريخ | الاسم | الهاتف | المبلغ | السبب | الحالة
```

#### ورقة `السحوبات_المقبولة`:
```
التاريخ | الاسم | الهاتف | المبلغ | السبب | تاريخ الموافقة
```

#### ورقة `السحوبات_المرفوضة`:
```
التاريخ | الاسم | الهاتف | المبلغ | السبب | تاريخ الرفض
```

---

### 4️⃣ إنشاء Google Apps Script

1. في Google Sheet، اضغط على **Extensions** (الإضافات)
2. اختر **Apps Script**
3. امسح الكود الموجود واستبدله بالكود التالي:

```javascript
function doPost(e) {
  try {
    const data = JSON.parse(e.postData.contents);
    const sheetName = data.sheet;
    const rowData = data.data;
    
    const ss = SpreadsheetApp.getActiveSpreadsheet();
    const sheet = ss.getSheetByName(sheetName);
    
    if (!sheet) {
      return ContentService.createTextOutput(JSON.stringify({
        success: false,
        message: 'Sheet not found: ' + sheetName
      }));
    }
    
    // تحويل البيانات إلى صف
    const headers = sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
    const row = headers.map(header => rowData[header] || '');
    
    sheet.appendRow(row);
    
    return ContentService.createTextOutput(JSON.stringify({
      success: true,
      message: 'Data added successfully'
    }));
    
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({
      success: false,
      message: error.toString()
    }));
  }
}
```

4. احفظ المشروع باسم: **Flosna API**

---

### 5️⃣ نشر السكريبت (Deploy)

1. اضغط على زر **Deploy** (أعلى اليمين)
2. اختر **New deployment**
3. اضغط على أيقونة الترس ⚙️ بجانب "Select type"
4. اختر **Web app**
5. املأ المعلومات:
   - **Description**: Flosna Data API
   - **Execute as**: Me
   - **Who has access**: Anyone
6. اضغط **Deploy**
7. **انسخ الرابط** الذي يظهر لك (Web app URL)
   - يبدأ بـ: `https://script.google.com/macros/s/...`

---

### 6️⃣ ربط الرابط بالموقع

1. افتح ملف `index.html`
2. ابحث عن السطر:
```javascript
scriptUrl: 'https://script.google.com/macros/s/...'
```
3. استبدل الرابط القديم بالرابط الجديد الذي نسخته
4. تأكد أن `enabled: true`

---

### 7️⃣ اختبار الربط

1. افتح الموقع
2. قم بإضافة إيداع تجريبي
3. تحقق من Google Sheet، يجب أن تجد البيانات ظهرت في ورقة `الإيداعات_المعلقة`

---

## 🎉 تم الربط بنجاح!

الآن كل البيانات التي تُضاف في الموقع ستُحفظ تلقائياً في Google Sheets.

---

## ⚠️ ملاحظات مهمة:

- **الأسماء يجب أن تكون بالضبط** (مع الشَّرطة السفلية _)
- **العناوين يجب أن تكون بنفس الترتيب**
- إذا غيّرت أي شيء في السكريبت، يجب عمل **Deploy جديد** واختيار **New deployment**
- احتفظ بالرابط في مكان آمن

---

## 🆘 حل المشاكل:

### المشكلة: البيانات لا تظهر في Google Sheet
- تأكد من أن الرابط صحيح
- تأكد من أن `enabled: true`
- تأكد من أن أسماء الأوراق صحيحة بالضبط
- افتح Console في المتصفح (F12) وتحقق من وجود أخطاء

### المشكلة: البيانات تذهب للورقة الخطأ
- تحقق من أسماء الأوراق
- تأكد من أن العناوين بنفس الترتيب

---

## 📱 ملاحظة عن Telegram:

إعدادات Telegram موجودة بالفعل في الكود:
```javascript
botToken: '8299247400:AAHOJU7qIanXJrvHEaryAkx5uL_MK8DTzOg'
chatId: '5773416612'
```

إذا أردت تغييرها، ابحث عن `TELEGRAM_CONFIG` في ملف `index.html`.
