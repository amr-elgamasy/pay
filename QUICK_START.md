# 🔧 خطوات التشغيل السريعة

## ⏱️ 5 دقائق فقط!

### 1️⃣ نسخ رابط Google Apps Script

بعد إنشاء Google Apps Script ونشره، انسخ الرابط الذي يظهر (يبدأ بـ `https://script.google.com/macros/s/...`)

### 2️⃣ تحديث الرابط في الكود

افتح ملف `index.html` وابحث عن السطر رقم **590** تقريباً:

```javascript
const GOOGLE_SHEETS_CONFIG = {
    enabled: true,
    scriptUrl: 'YOUR_GOOGLE_APPS_SCRIPT_URL_HERE' // ⚠️ الصق الرابط هنا
};
```

استبدل `YOUR_GOOGLE_APPS_SCRIPT_URL_HERE` برابطك

### 3️⃣ رفع التعديلات على GitHub

```bash
git add index.html
git commit -m "إضافة رابط Google Sheets"
git push origin main
```

### 4️⃣ تفعيل GitHub Pages

1. افتح https://github.com/amr-elgamasy/pay
2. اذهب إلى **Settings** (الإعدادات)
3. اختر **Pages** من القائمة الجانبية
4. في **Source** اختر **main** من القائمة
5. اضغط **Save**

انتظر دقيقة واحدة...

### 5️⃣ شارك الرابط! 🎉

الموقع أصبح متاح على:
```
https://amr-elgamasy.github.io/pay/
```

---

## ✅ اختبار سريع

1. افتح الرابط
2. أضف إيداع تجريبي
3. ادخل على **الإدارة** (admin / admin123)
4. افتح Google Sheet - يجب أن تجد البيانات!

---

## 🆘 مشاكل شائعة

### المشكلة: "Authorization required"
**الحل**: في Google Apps Script، اضغط زر ▶️ Run مرة واحدة ووافق على الأذونات

### المشكلة: البيانات لا تظهر في Google Sheets
**الحل**: تأكد من:
1. رابط Web App صحيح في index.html
2. أسماء الأوراق بالعربي مطابقة تماماً
3. Apps Script منشور كـ Web app وليس Add-on

### المشكلة: GitHub Pages لا يعمل
**الحل**: انتظر 5 دقائق، ثم جرب Ctrl+F5 (تحديث بدون cache)

---

## 🔒 تغيير كلمة المرور

في ملف `index.html` ابحث عن:

```javascript
function checkAdminLogin() {
    const username = document.getElementById('admin-username').value;
    const password = document.getElementById('admin-password').value;
    
    if (username === 'admin' && password === 'admin123') { // غيّر هنا
```

غيّر `admin123` إلى كلمة المرور الجديدة

---

**جاهز للاستخدام! 🚀**
