# دليل إعداد Google Sheets و Telegram

## 📋 جدول المحتويات
1. [إعداد Google Sheets](#إعداد-google-sheets)
2. [إعداد Telegram Bot](#إعداد-telegram-bot)
3. [تفعيل الميزات](#تفعيل-الميزات)

---

## 📊 إعداد Google Sheets

### الخطوة 1: إنشاء Google Sheet جديد
1. افتح [Google Sheets](https://sheets.google.com)
2. أنشئ ملف جديد
3. أنشئ 4 أوراق بالأسماء التالية:
   - `الإيداعات_المعلقة`
   - `الإيداعات_المقبولة`
   - `الإيداعات_المرفوضة`
   - `المصروفات`

### الخطوة 2: إضافة رؤوس الأعمدة

**ورقة الإيداعات_المعلقة:**
```
| التاريخ | الاسم | الهاتف | المبلغ | الحالة |
```

**ورقة الإيداعات_المقبولة:**
```
| التاريخ | الاسم | الهاتف | المبلغ | تاريخ الموافقة |
```

**ورقة الإيداعات_المرفوضة:**
```
| التاريخ | الاسم | الهاتف | المبلغ | تاريخ الرفض |
```

**ورقة المصروفات:**
```
| التاريخ | الوصف | الفئة | المبلغ |
```

### الخطوة 3: إنشاء Google Apps Script

1. في Google Sheets، اذهب إلى **Extensions** > **Apps Script**
2. امسح أي كود موجود والصق الكود التالي:

```javascript
function doPost(e) {
  try {
    const data = JSON.parse(e.postData.contents);
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(data.sheet);
    
    if (!sheet) {
      return ContentService.createTextOutput(JSON.stringify({
        status: 'error',
        message: 'Sheet not found: ' + data.sheet
      })).setMimeType(ContentService.MimeType.JSON);
    }
    
    // إضافة البيانات كصف جديد
    const values = Object.values(data.data);
    sheet.appendRow(values);
    
    return ContentService.createTextOutput(JSON.stringify({
      status: 'success',
      message: 'Data added successfully'
    })).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({
      status: 'error',
      message: error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

function doGet(e) {
  return ContentService.createTextOutput('Google Sheets API is running!');
}
```

3. احفظ المشروع باسم "Flosna API"
4. انقر على **Deploy** > **New deployment**
5. اختر نوع: **Web app**
6. اضبط الإعدادات:
   - **Execute as:** Me
   - **Who has access:** Anyone
7. انقر **Deploy**
8. **انسخ رابط Web app URL** - ستحتاجه لاحقاً

---

## 🤖 إعداد Telegram Bot

### الخطوة 1: إنشاء البوت
1. افتح Telegram وابحث عن [@BotFather](https://t.me/botfather)
2. أرسل الأمر: `/newbot`
3. اتبع التعليمات لإنشاء البوت
4. **احفظ Bot Token** (مثال: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### الخطوة 2: الحصول على Chat ID
1. ابحث عن [@userinfobot](https://t.me/userinfobot) في Telegram
2. أرسل أي رسالة للبوت
3. سيعطيك **Chat ID** (مثال: `987654321`)

**أو** للحصول على Chat ID لمجموعة:
1. أضف البوت الخاص بك إلى المجموعة
2. اذهب إلى الرابط: `https://api.telegram.org/bot<TOKEN>/getUpdates`
   (استبدل `<TOKEN>` بـ Bot Token الخاص بك)
3. ابحث عن `"chat":{"id":` وانسخ الرقم

---

## ⚙️ تفعيل الميزات

### في ملف `index.html` ابحث عن:

```javascript
const GOOGLE_SHEETS_CONFIG = {
    enabled: false, // غير الى true بعد الإعداد
    scriptUrl: '', // ضع رابط Google Apps Script Web App هنا
};

const TELEGRAM_CONFIG = {
    enabled: false, // غير الى true بعد الإعداد
    botToken: '', // ضع Bot Token هنا
    chatId: '', // ضع Chat ID هنا
};
```

### غير الإعدادات إلى:

```javascript
const GOOGLE_SHEETS_CONFIG = {
    enabled: true, // ✅ فعّل Google Sheets
    scriptUrl: 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec', // ✅ الصق الرابط
};

const TELEGRAM_CONFIG = {
    enabled: true, // ✅ فعّل Telegram
    botToken: '123456789:ABCdefGHIjklMNOpqrsTUVwxyz', // ✅ الصق Token
    chatId: '987654321', // ✅ الصق Chat ID
};
```

---

## 🎯 ما تم تنفيذه

### ✅ Google Sheets
سيتم إرسال البيانات تلقائياً إلى 4 أوراق منفصلة:
- **الإيداعات_المعلقة**: كل إيداع جديد
- **الإيداعات_المقبولة**: الإيداعات بعد الموافقة
- **الإيداعات_المرفوضة**: الإيداعات المرفوضة
- **المصروفات**: جميع المصروفات

### ✅ Telegram Notifications
سيصل إشعار فوري في الحالات التالية:
- ✅ **عند الموافقة على إيداع** (بعد التأكيد فقط)
- ✅ **عند إضافة مصروف**

**لن يصل إشعار:**
- ❌ عند إضافة إيداع معلق (قبل الموافقة)
- ❌ عند رفض إيداع

---

## 🧪 اختبار الإعداد

### اختبار Google Sheets:
1. أضف إيداع جديد في التطبيق
2. تحقق من ورقة "الإيداعات_المعلقة"
3. وافق على الإيداع
4. تحقق من ورقة "الإيداعات_المقبولة"

### اختبار Telegram:
1. وافق على إيداع معلق
2. يجب أن يصل إشعار فوري للبوت
3. أضف مصروف جديد
4. يجب أن يصل إشعار فوري للبوت

---

## 🔧 استكشاف الأخطاء

### إذا لم تصل البيانات إلى Google Sheets:
1. تأكد من أن رابط `scriptUrl` صحيح
2. تأكد من أن أسماء الأوراق مطابقة تماماً (بالعربية)
3. افتح Console في المتصفح (F12) وتحقق من الأخطاء

### إذا لم تصل إشعارات Telegram:
1. تأكد من أن `botToken` و `chatId` صحيحان
2. تأكد من أنك بدأت محادثة مع البوت (أرسل `/start`)
3. افتح Console في المتصفح (F12) وتحقق من الأخطاء

---

## 📝 ملاحظات مهمة

- ⚠️ **الأمان**: لا تشارك Bot Token مع أحد
- 🔄 **التحديث التلقائي**: البيانات تُرسل فوراً بعد كل عملية
- 💾 **النسخ الاحتياطي**: البيانات محفوظة محلياً في المتصفح أيضاً
- 🌐 **الإنترنت**: يحتاج التطبيق اتصال إنترنت لإرسال البيانات

---

تم بنجاح! 🎉
